#include <stdio.h>


int main(void)
{
    char a = 'H';
    char b = 'I';
    char c = '!';
    printf("%c %c %c\n", a, b, c);
    // printing chars as integers is a process known as "casting".  This also works with printing integers as "chars";
    printf("%i %i %i\n",(int) a, (int) b, (int) c);
    int d = 72;
    int e = 73;
    int f = 33;
    printf("%c %c %c\n", (char) d, (char) e, (char) f);
    // return(

    // )
}